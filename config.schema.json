{
  "pluginAlias": "Luftdaten",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "<p align=\"center\"><img src=\"https://raw.githubusercontent.com/rafalr100/homebridge-luftdaten/main/docs/icon.png\" width=\"80\" alt=\"homebridge-luftdaten\"></p>\n\nReads Luftdaten / Sensor.Community (airrohr) sensors — local-first, with a cloud fallback. Add one entry per sensor; each needs a **Local URL** and/or a **Sensor ID**.",
  "footerDisplay": "See the [README](https://github.com/rafalr100/homebridge-luftdaten) for details. Pressure is logged but not exposed to HomeKit.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "Luftdaten",
        "required": true,
        "description": "Name for this platform (shown in the Homebridge logs)."
      },
      "sensors": {
        "title": "Sensors",
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "title": "Name",
              "type": "string",
              "default": "Luftdaten",
              "required": true,
              "description": "Name shown in the Home app."
            },
            "localUrl": {
              "title": "Local URL",
              "type": "string",
              "format": "uri",
              "placeholder": "http://192.168.1.50/data.json",
              "description": "The sensor's local data endpoint. Tried first."
            },
            "sensorId": {
              "title": "Sensor.Community Sensor ID",
              "type": "string",
              "placeholder": "12345",
              "description": "Numeric ID of the SDS011 sensor, used for the cloud fallback."
            },
            "pollInterval": {
              "title": "Poll interval (seconds)",
              "type": "integer",
              "default": 120,
              "minimum": 10,
              "description": "How often to read the sensor."
            },
            "requestTimeout": {
              "title": "Request timeout (seconds)",
              "type": "integer",
              "default": 10,
              "minimum": 1,
              "description": "Per-request timeout."
            },
            "hasTempSensor": {
              "title": "Has temperature / humidity sensor",
              "type": "boolean",
              "default": true,
              "description": "Add temperature and humidity services (SHT3X / BME280)."
            }
          }
        }
      }
    }
  },
  "layout": [
    "name",
    {
      "key": "sensors",
      "type": "array",
      "title": "Sensors",
      "buttonText": "Add sensor",
      "items": [
        "sensors[].name",
        "sensors[].localUrl",
        "sensors[].sensorId",
        {
          "type": "fieldset",
          "title": "Advanced",
          "expandable": true,
          "expanded": false,
          "items": [
            "sensors[].pollInterval",
            "sensors[].requestTimeout",
            "sensors[].hasTempSensor"
          ]
        }
      ]
    }
  ]
}
